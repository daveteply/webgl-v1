import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.daveteply.webgl01',
  appName: 'webgl-v1',
  webDir: 'dist/webgl-v1',
  bundledWebRuntime: false
};

export default config;
