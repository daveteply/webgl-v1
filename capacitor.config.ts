import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.turbogeekbear.rikkle01',
  appName: 'webgl-v1',
  webDir: 'dist/webgl-v1',
  bundledWebRuntime: false,
};

export default config;
